/**
 * Fixture for buildWebSiteNode(). Always-emit, no params.
 */
export const websiteFixture = {
  builderName: 'buildWebSiteNode',
  nodeType: 'WebSite',
  alwaysEmits: true,
  full: { input: undefined },
  missingRequired: undefined,
} as const;
