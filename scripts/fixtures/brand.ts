/**
 * Fixture for buildBrandNode(). Always-emit, no params.
 */
export const brandFixture = {
  builderName: 'buildBrandNode',
  nodeType: 'Brand',
  alwaysEmits: true,
  full: { input: undefined },
  missingRequired: undefined,
} as const;
